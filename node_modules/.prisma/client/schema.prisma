// schema.prisma

generator client {
  provider = "prisma-client-js"
  previewFeatures = ["jsonProtocol"]
}

datasource db {
  provider = "postgresql"
  url = "postgres://default:lzF08BMAduNY@ep-plain-silence-317050-pooler.us-east-1.postgres.vercel-storage.com:5432/verceldb"  //env("POSTGRES_PRISMA_URL") // uses connection pooling
  directUrl = "postgres://default:lzF08BMAduNY@ep-plain-silence-317050.us-east-1.postgres.vercel-storage.com:5432/verceldb"//env("POSTGRES_URL_NON_POOLING") // uses a direct connection
  shadowDatabaseUrl = "postgres://default:lzF08BMAduNY@ep-plain-silence-317050.us-east-1.postgres.vercel-storage.com:5432/verceldb"//env("POSTGRES_URL_NON_POOLING") // used for migrations
}

model Tache {
  id        String     @default(cuid()) @id
  title     String
  description String?
  status   String
}
